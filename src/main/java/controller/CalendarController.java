package controller;

/**
 * Interface for the CalendarController class. This interface is used to define the methods that the
 * CalendarController class must implement.
 */
public interface CalendarController {

  /**
   * Method to start the application. Called to start the application. Sets up the application in
   * correct mode and starts the application.
   *
   * @param startAppArgs the arguments to start the application with.
   */
  void startApp(String[] startAppArgs);

}
